MIRO_CLIENT_ID=
MIRO_CLIENT_SECRET=
# Optional override. If omitted, backend uses http://localhost:3000/api/auth/miro/callback in dev
# and infers host on deployment.
MIRO_REDIRECT_URI=http://localhost:3000/api/auth/miro/callback

# Optional but recommended for persistence. If omitted, app falls back to in-memory storage.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
